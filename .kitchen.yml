---
provisioner:
  name: chef_zero
  data_bag_path: test/integration/data_bags

suites:
  - name: default
    attributes:
      users:
        - test1-staging
    run_list:
      - recipe[osl-app::default]
  - name: systemd
    run_list:
      - recipe[osl-app::systemd]
